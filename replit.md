# Versus (legacy Replit notes)

See **[README.md](./README.md)** for local Windows/macOS/Linux setup.

## Quick local commands

- `corepack pnpm setup` — install + init SQLite
- `corepack pnpm dev` — API `:3001` + web `:5173`
- `corepack pnpm smoke` — API health + demo login

## Product API

Real domain API lives in `versus-backend/` (not `artifacts/api-server`).

## Demo accounts

All passwords: `versus123` — tags `@alex_god`, `@clement_boss`, `@hugo_fast`, `@sarah_smash`, `@thomas_pro`.
